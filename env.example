# 資料庫配置
# 使用MySQL
DB_TYPE=mysql
DB_HOST=localhost
DB_PORT=3306
DB_NAME=visitor_db
DB_USER=visitor_user
DB_PASSWORD=your_secure_password_here

# 加密金鑰（32字元）
# 請使用 openssl rand -base64 32 生成
ENCRYPTION_KEY=your-32-character-secret-key-here!

# 伺服器端口
PORT=3000

# 訪客證有效期（小時）
VISITOR_PASS_EXPIRY_HOURS=1

# 外部SenseLink API
SENSELINK_API_BASE_URL=http://facereg.aoy.tw/sl
APP_KEY=c6324cfa50169e85
APP_SECRET=e30af86f8f4e75d128ba4288597dea3c

# SenseLink 登入資訊（用於通過 API 自動獲取 auth-token）
SENSELINK_USERNAME=admin1234
SENSELINK_PASSWORD=GP1234as

# 手動提供的 auth-token（可選，如果提供則不使用 API 登入）
# 注意：設備登入返回的是 token-device-* 格式，web 端可能需要 token-web-* 格式
# AUTH_TOKEN=token-web-1:xxxxx
# AUTH_TIMESTAMP=1761827598669
# AUTH_SIGN=xxxxx

# JWT簽名密鑰（用於管理員認證）
JWT_SECRET=your-jwt-secret-key

# 設備識別碼
DEVICE_LDID=TEST-DEVICE-001

